import Vue from 'vue'

import { Row, Col, Button, Grid, GridItem, Swipe, SwipeItem, Lazyload, Icon, Image, Tab, Tabs, Field, CellGroup, Notify, ImagePreview, NoticeBar, Rate, Toast } from 'vant'

Vue.use(Row)
  .use(Col)
  .use(Button)
  .use(Grid)
  .use(GridItem)
  .use(Swipe)
  .use(SwipeItem)
  .use(Lazyload)
  .use(Icon)
  .use(Image)
  .use(Tab)
  .use(Tabs)
  .use(Field)
  .use(CellGroup)
  .use(Notify)
  .use(ImagePreview)
  .use(NoticeBar)
  .use(Rate)
  .use(Toast)
